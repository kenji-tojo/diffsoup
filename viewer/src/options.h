// options.h — Runtime rendering options.

#pragma once

namespace viewer {

struct Options {
    bool  render_geometry       = true;
    float background_brightness = 0.f;
};

}  // namespace viewer
